# What I built
Developed core management script `manage_daily.py` using Python subprocess.

# What I learned
- Python `subprocess.run` enables clean execution of Git commands.

# What's next
- Add validation checks for template placeholders.